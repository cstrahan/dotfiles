---
# Tool installation dispatcher - routes to OS-specific implementation

- name: Install tools (Linux)
  ansible.builtin.include_tasks: tools_linux.yml
  when: ansible_os_family == "Debian"

- name: Install tools (macOS)
  ansible.builtin.include_tasks: tools_darwin.yml
  when: ansible_os_family == "Darwin"
